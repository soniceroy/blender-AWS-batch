import boto3
def main(event, context):
    """
    upload content to an s3 bucket.
    :param event:
    :param context:
    :return: successful or unsucceful http response
    """
    if True:
        return {
            "statusCode": 200,
            "body": {"message": "success"}
        }
